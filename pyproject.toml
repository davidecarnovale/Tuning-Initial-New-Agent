[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "ecu-map-agent"
version = "0.1.0"
description = "ECU parser and map classifier"
authors = [{name="Your Name"}]
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
